@model Eventra.Models.ViewModels.ChangePasswordViewModel
@{
    ViewData["Title"] = "Change Password";
}

@section Styles {
    <link rel="stylesheet" href="~/css/styleeditprofile.css" asp-append-version="true" />
}

<main class="edit-page">
    <div class="container edit-wrapper">

        <section class="edit-left">
            <p class="eyebrow">Account security</p>
            <h1>Change Password</h1>
            <p class="intro-text">
                Choose a strong password. It must be at least 8 characters and contain at least one uppercase letter and one number.
            </p>
        </section>

        <section class="edit-card">
            <h2>Update Password</h2>
            <p class="card-subtitle">Enter your current password, then set a new one</p>

            <form asp-action="ChangePassword" method="post" class="edit-form">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:12px;"></div>

                <div class="input-group">
                    <label asp-for="CurrentPassword">Current Password</label>
                    <div class="pw-wrapper">
                        <input asp-for="CurrentPassword" placeholder="Your current password" />
                        <button type="button" class="pw-toggle" onclick="togglePw(this)" aria-label="Toggle visibility">
                            <svg class="eye-icon eye-closed" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>
                            <svg class="eye-icon eye-open" style="display:none" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        </button>
                    </div>
                    <span asp-validation-for="CurrentPassword" class="text-danger"></span>
                </div>

                <div class="input-group">
                    <label asp-for="NewPassword">New Password</label>
                    <div class="pw-wrapper">
                        <input asp-for="NewPassword" placeholder="At least 8 characters" />
                        <button type="button" class="pw-toggle" onclick="togglePw(this)" aria-label="Toggle visibility">
                            <svg class="eye-icon eye-closed" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>
                            <svg class="eye-icon eye-open" style="display:none" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        </button>
                    </div>
                    <span asp-validation-for="NewPassword" class="text-danger"></span>
                </div>

                <div class="input-group">
                    <label asp-for="ConfirmPassword">Confirm New Password</label>
                    <div class="pw-wrapper">
                        <input asp-for="ConfirmPassword" placeholder="Repeat your new password" />
                        <button type="button" class="pw-toggle" onclick="togglePw(this)" aria-label="Toggle visibility">
                            <svg class="eye-icon eye-closed" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>
                            <svg class="eye-icon eye-open" style="display:none" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        </button>
                    </div>
                    <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                </div>

                <div class="form-actions">
                    <button type="submit" class="save-btn">Update Password</button>
                    <a asp-controller="Profile" asp-action="Edit" class="cancel-btn">Cancel</a>
                </div>
            </form>
        </section>

    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <style>
        .pw-wrapper { position: relative; }
        .pw-wrapper input { width: 100%; padding-right: 44px; }
        .pw-toggle { position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: none; border: none; cursor: pointer; padding: 4px; color: var(--text-soft); line-height: 0; }
        .pw-toggle:hover { color: var(--primary); }
        .card-subtitle { color: var(--text-soft); margin-bottom: 24px; font-size: 0.95rem; }
    </style>
    <script>
        function togglePw(btn) {
            var input = btn.parentElement.querySelector('input');
            var closed = btn.querySelector('.eye-closed');
            var open = btn.querySelector('.eye-open');
            if (input.type === 'password') {
                input.type = 'text';
                closed.style.display = 'none';
                open.style.display = '';
            } else {
                input.type = 'password';
                closed.style.display = '';
                open.style.display = 'none';
            }
        }
    </script>
}
